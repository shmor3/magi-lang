[package]
id = "calculator"
name = "Calculator"
version = "0.1.0"
description = "Math calculator demonstrating functions, recursion, and the shared module"
authors = ["shmor3"]
magi = ">=0.2.0"

[dependencies]
shared = { path = "../shared" }
