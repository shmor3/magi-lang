[package]
id = "hello"
name = "Hello MAGI"
version = "0.1.0"
description = "Hello world demonstrating imports, functions, and string formatting"
authors = ["shmor3"]
magi = ">=0.2.0"

[dependencies]
shared = { path = "../shared" }
