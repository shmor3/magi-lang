[package]
name = "magi-lang"
version = "0.9.0"
edition = "2021"
description = "MAGI programming language — compiler, runtime, interpreter, LSP, and CLI"

[dependencies]
# Zero dependencies

[[bin]]
name = "magi"
path = "src/bin/magi.rs"
